-- Must have to be part of Prime's cult
return {
  "ThePrimeagen/harpoon",
  branch = "harpoon2",
  dependencies = { "nvim-lua/plenary.nvim" },
  opts = {

    settings = {
      save_on_toggle = true, -- as the name say, save the new list order on toggle
    },
  },
}
